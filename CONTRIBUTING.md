# 贡献指南

感谢有兴趣改进本 skill。本 skill 是一份**可执行的流程规范**，不是知识库，
因此对"事实准确性"和"可验证性"的要求高于一般文档。

## 目录约定

| 位置 | 放什么 | 不放什么 |
|---|---|---|
| `SKILL.md` | 入口：流程、Output Contract、硬性红线、经验教训 | 长篇方法论细节 |
| `references/` | 按阶段拆分的方法论全文 | 可执行代码 |
| `scripts/` | 可独立运行的 Python 脚本，每个脚本可单独调用 | 依赖私有数据或网络 |
| `evals/` | 触发 / 验收 / 边界三类评测 | 真实项目数据 |
| `assets/` | 已脱敏的案例与方法论决策链 | 真实专利文本、附图、申请人/发明人信息 |

**渐进式披露原则**：SKILL.md 只给流程与契约，细节放 `references/`，
被调用时才加载。不要把方法论全文堆进 SKILL.md。

## 提交前必须做的三件事

```bash
# 1. 自检（必须全部通过）
python self_test.py

# 2. JSON 可解析
python -c "import json;json.load(open('evals/evals.json',encoding='utf-8'))"

# 3. 敏感信息自查：不得出现任何真实项目信息
#    （发明人姓名、申请人名称、申请号、专利标题、单位名称、项目名称）
```

依赖：`python>=3.10`，`pip install python-docx matplotlib numpy`。

## 内容修改的硬性要求

1. **事实必须可溯源**：涉及法条、期限、费用标准、系统操作路径的内容，
   须同时给出依据（法条条号、规定名称、办事指南名称或官方答复文号）。
   不确定的写"以官方现行规定为准"，不要凭印象下断言。
2. **区分"法定"与"实务"**：法定期限、减缴比例等是硬约束；
   发文时长、系统界面路径等属于实务口径，须注明会随系统改版变化。
3. **改 `description` 后必须过一遍 evals**：`description` 是触发入口，
   语境不全导致 undertrigger（该用不用），边界不写清导致 scope creep（不该用乱用）。
   同时检查长度不超过 1024 字符。
4. **改流程后同步四处**：`SKILL.md` 流程段、`SKILL.md` 目录结构、
   `README.md` 目录树、`CHANGELOG.md`。
5. **新增/修改脚本后**：补进 `self_test.py` 的正反用例，不允许只测 happy path。

## 提 Issue 时请附上

- 你使用的 agent 与版本、`python` 版本
- 触发失败的具体 prompt 原文（如果是未触发/误触发）
- 期望行为与实际行为的差异
- 若涉及 CNIPA 系统：截图时**务必遮蔽申请号、发明人、申请人等信息**

## 关于保密（最重要的一条）

本仓库是公开仓库。**请勿在 Issue、PR、Discussion 或任何附带文件中提交**：

- 任何未公开的专利申请文本、权利要求、说明书、附图
- 申请号、案卷编号、发明人姓名、申请人名称、单位名称
- 未公开的技术方案、实验数据、参数

需要展示问题时，请用最小化的虚构样例复现。违反此条的提交会被直接关闭。
